# Agents

Start with [`CLAUDE.md`](CLAUDE.md) — it is the canonical map (read order, important files, `output/` layout, conventions). This file exists so tools that look for `AGENTS.md` still land in the right place.
